msg: i am welcome-api from config-server
server:
  port: 8090

spring:
  application:
    name: WELCOME-API

  boot:
    admin:
      client:
        url: http://localhost:1111


management:
  endpoints:
    web:
      exposure:
        include: '*'
